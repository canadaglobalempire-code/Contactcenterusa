import type { Metadata } from "next";
import Content from "./Content";

export const metadata: Metadata = {
  title: "Outbound Call Center Services | Telemarketing & Win-Back Campaigns",
  description:
    "Drive revenue with TCPA-compliant outbound calling campaigns including telemarketing, customer surveys, win-back programs, and upselling. 22% average conversion rate with US-based agents.",
};

export default function Page() {
  return <Content />;
}
